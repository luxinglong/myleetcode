#include<iostream>

using namespace std;

// method one which can achieve O(N^3)

int maxSubsequenceSum1(int a[], int size, int &start, int &end)
{
	int maxSum = 0;
	for (int i = 0; i < size; i++)
		for (int j = i; j < size; j++)
		{
			
		}
	return maxSum;
}

int main(int argc, char **argv)
{
	
	return 0;
}
